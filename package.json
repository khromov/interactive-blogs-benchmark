{
  "name": "interactive-blogs-benchmark",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Benchmark harness comparing client JS and Core Web Vitals for interactive-blog frameworks (islands vs whole-page hydration).",
  "scripts": {
    "benchmark": "node benchmark.mjs --gzip --out results/latest-run.md --json results/latest-run.json",
    "benchmark:identity": "node benchmark.mjs --out results/latest-run.md --json results/latest-run.json"
  },
  "dependencies": {
    "chrome-launcher": "^1.2.0",
    "lighthouse": "^13.4.1"
  }
}
